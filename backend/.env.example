# Firebase Admin SDK Service Account
# 從 Firebase Console > Project Settings > Service Accounts 下載 JSON 並放到 serviceAccount.json
FIREBASE_PROJECT_ID=ck-cl-24edb

# 服務器配置
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# 開發模式設定（僅在本地開發時使用）
DEV_MODE=true
DEV_BYPASS_TOKEN=dev-admin-token-12345

# 允許的教育網域 (台北市教育帳號)
ALLOWED_EMAIL_DOMAIN=tp.edu.tw

# Email Configuration (Optional - if not set, email notifications will be disabled)
# For Gmail: use your Gmail address and an App Password (not your regular password)
# Generate App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# System Email (default to SMTP_USER if not set)
SYSTEM_EMAIL=noreply@cksh-club.tw
